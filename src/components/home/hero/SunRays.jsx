import styles from './SunRays.module.css'

// Warm, slowly rotating/pulsing sunlight glow blended behind the hero imagery
export default function SunRays() {
  return <div className={styles.sunRays} aria-hidden="true" />
}
